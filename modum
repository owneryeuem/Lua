local TweenService = game:GetService("TweenService")
local gui = Instance.new("ScreenGui", game.CoreGui)
local holder = Instance.new("Frame")
holder.Parent = gui
holder.AnchorPoint = Vector2.new(.5,.5)
holder.Position = UDim2.fromScale(.5,.5)
holder.Size = UDim2.new(0,0,0,120)
holder.BackgroundTransparency = 1
holder.ClipsDescendants = true
local text = Instance.new("TextLabel")
text.Parent = holder
text.Size = UDim2.new(0,700,1,0)
text.Position = UDim2.new(.5,-350,0,0)
text.BackgroundTransparency = 1
text.Text = "│</> Script Loading... │"
text.Font = Enum.Font.FredokaOne
text.TextScaled = true
text.TextColor3 = Color3.new(1,1,1)
text.TextStrokeTransparency = 0
local open = TweenService:Create(
    holder,
    TweenInfo.new(0.8, Enum.EasingStyle.Quart),
    {Size = UDim2.new(0,700,0,120)}
)
open:Play()
open.Completed:Wait()
task.wait(1.5)
local close = TweenService:Create(
    holder,
    TweenInfo.new(0.8, Enum.EasingStyle.Quart),
    {Size = UDim2.new(0,0,0,120)}
)
close:Play()
close.Completed:Wait()
gui:Destroy()
